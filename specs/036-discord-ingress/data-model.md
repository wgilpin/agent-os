# Data Model

No new persistent data models are required for this feature.
The ingress acts purely as a transient gateway, transforming external websocket JSON payloads into internal Elixir messages.
