# Tasks: Stage 2 Write the Manifest (write-manifest)

**Input**: Design documents from `/specs/013-write-manifest/`
**Prerequisites**: plan.md (required), spec.md (required), research.md, data-model.md, contracts/

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Project initialization and basic structure

- [x] T001 Create projection module skeleton at lib/agent_os/manifest/projection.ex
- [x] T002 Configure formatting and linter guidelines for the new projection files

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core infrastructure that MUST be complete before ANY user story can be implemented

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [x] T003 Implement unit test file structure at test/agent_os/manifest/projection_test.exs

**Checkpoint**: Foundation ready - user story implementation can now begin

---

## Phase 3: User Story 1 - Project confirmed spec to manifest (Priority: P1) 🎯 MVP

**Goal**: Project a confirmed ElicitedSpec into a schema-valid v2 manifest.

**Independent Test**: Test case in `test/agent_os/manifest/projection_test.exs` projects a valid `ElicitedSpec` and checks that the result parses and validates via `AgentOS.Manifest.load/1` and runs deterministically.

### Tests for User Story 1

> **NOTE: Write these tests FIRST, ensure they FAIL before implementation**

- [x] T004 [P] [US1] Write test in test/agent_os/manifest/projection_test.exs for projecting a valid, confirmed ElicitedSpec

### Implementation for User Story 1

- [x] T005 [US1] Implement spec-to-manifest struct projection in lib/agent_os/manifest/projection.ex
- [x] T006 [US1] Implement YAML frontmatter serialization in lib/agent_os/manifest/projection.ex
- [x] T007 [US1] Implement file writer in lib/agent_os/manifest/projection.ex

**Checkpoint**: User Story 1 is functional and verifiable independently.

---

## Phase 4: User Story 2 - Reject unconfirmed spec at boundary (Priority: P1)

**Goal**: Refuse to emit a manifest if the spec's `confirmed` flag is not `true`.

**Independent Test**: Test case in `test/agent_os/manifest/projection_test.exs` passes a spec with `confirmed: false` or unset and asserts it returns `{:error, :not_confirmed}`.

### Tests for User Story 2

- [x] T008 [P] [US2] Write test in test/agent_os/manifest/projection_test.exs for rejecting unconfirmed specs

### Implementation for User Story 2

- [x] T009 [US2] Implement confirmed flag validation guard in lib/agent_os/manifest/projection.ex

**Checkpoint**: User Story 2 is functional and verifiable independently.

---

## Phase 5: User Story 3 - Show human consent view of manifest (Priority: P1)

**Goal**: Render a capability consent view from the projected manifest.

**Independent Test**: Test case in `test/agent_os/manifest/projection_test.exs` projects a spec, renders the consent view, and asserts it matches the format generated by `AgentOS.CapabilityRender`.

### Tests for User Story 3

- [x] T010 [P] [US3] Write test in test/agent_os/manifest/projection_test.exs for rendering consent view from manifest

### Implementation for User Story 3

- [x] T011 [US3] Implement consent view renderer function in lib/agent_os/manifest/projection.ex

**Checkpoint**: User Story 3 is functional and verifiable independently.

---

## Phase 6: User Story 4 - Preserve minimisation & error on under-specification (Priority: P2)

**Goal**: Enforce strict minimisation, fail on unknown connectors, and raise errors on missing required fields.

**Independent Test**: Test cases in `test/agent_os/manifest/projection_test.exs` passing specs with unknown connectors or missing required fields assert that the projector returns errors and writes no files.

### Tests for User Story 4

- [x] T012 [P] [US4] Write test in test/agent_os/manifest/projection_test.exs for unknown capability and under-specification validation

### Implementation for User Story 4

- [x] T013 [US4] Implement connector registry checks and missing field error raising in lib/agent_os/manifest/projection.ex

**Checkpoint**: User Story 4 is functional and verifiable independently.

---

## Phase 7: User Story 5 - Keep manifest invisible to agent (Priority: P3)

**Goal**: Ensure manifest is written only to host-side substrate directory.

**Independent Test**: Test case in `test/agent_os/manifest/projection_test.exs` asserts the manifest output path is restricted to host-side locations.

### Tests for User Story 5

- [x] T014 [P] [US5] Write test in test/agent_os/manifest/projection_test.exs checking output file destination is substrate-only

### Implementation for User Story 5

- [x] T015 [US5] Implement directory output validation to ensure manifests are saved to local manifests/ directory in lib/agent_os/manifest/projection.ex

**Checkpoint**: User Story 5 is functional and verifiable independently.

---

## Phase N: Polish & Cross-Cutting Concerns

**Purpose**: Formatter and quality checks

- [x] T016 Run formatting and check credo warnings for lib/agent_os/manifest/projection.ex
- [x] T017 Run mix test to verify all tests pass
- [x] T018 Run dialyzer check for new code

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies - can start immediately.
- **Foundational (Phase 2)**: Depends on Setup completion - BLOCKS all user stories.
- **User Stories (Phase 3+)**: All depend on Foundational phase completion.
  - User stories can then proceed in parallel (if staffed) or sequentially in priority order.
- **Polish (Final Phase)**: Depends on all desired user stories being complete.

### User Story Dependencies

- **User Story 1 (P1)**: Can start after Foundational (Phase 2) - No dependencies on other stories.
- **User Story 2 (P2)**: Can start after Foundational (Phase 2) - Independent.
- **User Story 3 (P3)**: Can start after Foundational (Phase 2) - Independent.

### Parallel Opportunities

- Setup tasks can run in parallel.
- Test files can be initialized concurrently.
- All tests marked `[P]` (T004, T008, T010, T012, T014) can be written in parallel.

---

## Parallel Example: User Story 1

```bash
# Launch test and struct projection definition in parallel:
Task: "Write test in test/agent_os/manifest/projection_test.exs for projecting a valid, confirmed ElicitedSpec"
Task: "Implement spec-to-manifest struct projection in lib/agent_os/manifest/projection.ex"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Setup and Foundational phases.
2. Complete Phase 3: User Story 1 (Write test first, then implement).
3. Validate User Story 1 independently using ExUnit.
