# Tasks: Stage 6 Deploy-on-Green

**Input**: Design documents from `/specs/017-deploy-on-green/`
**Prerequisites**: plan.md (required), spec.md (required for user stories), research.md, data-model.md, contracts/

**Tests**: Backend unit tests are required as part of ExUnit test-driven development.

**Organization**: Tasks are grouped by user story to enable independent implementation and testing of each story.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Project initialization and basic structure

- [x] T001 Create project structure and verify that all speckit design docs are present in specs/017-deploy-on-green/

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core infrastructure that MUST be complete before ANY user story can be implemented

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [x] T002 Implement code_hash/2 and code_hash/1 helper functions in lib/agent_os/provisioner.ex
- [x] T003 [P] Update AgentOS.Pipeline.Stage5.Verdict struct and compute code_hash in lib/agent_os/pipeline/stage5_review.ex
- [x] T004 [P] Update persist_verdict/3 to calculate and store the code hash in lib/agent_os/pipeline/stage3_judge.ex

**Checkpoint**: Foundation ready - user story implementation can now begin

---

## Phase 3: User Story 1 - Both Checks Pass, Deploy Proceeds Under Review Mode (Priority: P1) 🎯 MVP

**Goal**: Gate deployment on co-generated judge + security review passes and hand off to the existing review-mode rail.

**Independent Test**: Mock passing verdicts with matching code hashes and verify all three review modes function correctly.

### Implementation for User Story 1

- [x] T005 [US1] Update record_provenance/4 to accept and record verdicts in lib/agent_os/provisioner.ex
- [x] T006 [US1] Modify deploy/3 to retrieve verdicts, check pass status/hash, and log to provenance in lib/agent_os/provisioner.ex
- [x] T007 [US1] Add unit tests in test/agent_os/provisioner_test.exs for the green path under all three review modes

**Checkpoint**: At this point, User Story 1 should be fully functional and testable independently.

---

## Phase 4: User Story 2 - Either Check Fails, Deploy Is Blocked Regardless of Review Mode (Priority: P1)

**Goal**: Gate deployment on failure cases (fails, missing, stale/mismatched code hashes) and report details in the standing inventory.

**Independent Test**: Mock failing/missing/stale verdicts and verify deploy is blocked under all review modes, with failure reasons shown in the standing inventory.

### Implementation for User Story 2

- [x] T008 [US2] Update deploy/3 to handle gate failures and record failure reasons in lib/agent_os/provisioner.ex
- [x] T009 [US2] Modify render/1 to format and print the exact failing checks in lib/agent_os/inventory.ex
- [x] T010 [US2] Add unit tests in test/agent_os/provisioner_test.exs for all deploy gate failure paths
- [x] T011 [US2] Add unit tests in test/agent_os/inventory_test.exs for rendering of failed checks and reasons

**Checkpoint**: At this point, User Stories 1 and 2 should both work independently.

---

## Phase 5: User Story 3 - Manifest Invisibility Holds for a Machine-Written Manifest (Priority: P2)

**Goal**: Re-verify that the manifest invisibility and runtime-gate invariants still hold for machine-written manifests co-generated by Stage 2.

**Independent Test**: Run the existing world-B verification suite using a machine-written manifest.

### Implementation for User Story 3

- [x] T012 [US3] Update test cases in test/agent_os/world_b_test.exs to verify manifest invisibility with a machine-written manifest

**Checkpoint**: All user stories should now be independently functional.

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Improvements that affect multiple user stories

- [x] T013 [P] Run mix format and mix credo on the codebase
- [x] T014 Run quickstart validation to verify inventory render output

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies - can start immediately
- **Foundational (Phase 2)**: Depends on Setup completion - BLOCKS all user stories
- **User Stories (Phase 3+)**: All depend on Foundational phase completion
- **Polish (Final Phase)**: Depends on all desired user stories being complete

### User Story Dependencies

- **User Story 1 (P1)**: Can start after Foundational (Phase 2) - No dependencies on other stories
- **User Story 2 (P2)**: Can start after Foundational (Phase 2) - May integrate with US1 but should be independently testable
- **User Story 3 (P3)**: Can start after Foundational (Phase 2) - May integrate with US1/US2 but should be independently testable

### Parallel Opportunities

- Setup task T001
- Foundational tasks T003 and T004 can run in parallel with each other (different files)
- Once Foundational phase completes, User Stories 1, 2, and 3 can be developed independently or sequentially

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup
2. Complete Phase 2: Foundational (CRITICAL)
3. Complete Phase 3: User Story 1
4. **STOP and VALIDATE**: Test User Story 1 independently
