---
phase: 01-walking-skeleton-v0
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - config/config.exs
  - lib/agent_os/scheduler.ex
  - lib/agent_os/provisioner.ex
  - lib/agent_os/application.ex
  - test/agent_os/scheduler_test.exs
  - test/agent_os/provisioner_test.exs
autonomous: true
requirements:
  - REQ-trigger-time
  - REQ-instantiate-from-declaration

must_haves:
  truths:
    - "A self-rescheduling GenServer computes the milliseconds until the next 07:00 and arms a timer"
    - "When the timer fires, it triggers a run AND schedules the next day's 07:00"
    - "The single discovery agent is provisioned from hard-wired config (NOT from an arbitrary manifest), and a startup check warns if config and manifest drift"
  artifacts:
    - path: "lib/agent_os/scheduler.ex"
      provides: "Daily 07:00 self-rescheduling timer GenServer"
      contains: "Process.send_after"
    - path: "lib/agent_os/provisioner.ex"
      provides: "Hard-wired provisioning + manifest-drift consistency check"
      contains: "def agent_config"
    - path: "config/config.exs"
      provides: "Hard-wired agent config (timezone, manifest path, agent command)"
      contains: "config :agent_os"
  key_links:
    - from: "lib/agent_os/scheduler.ex"
      to: "next 07:00"
      via: "ms_until_next_0700 + Process.send_after(self(), :fire, ms)"
      pattern: "send_after"
    - from: "lib/agent_os/provisioner.ex"
      to: "manifests/discovery.md"
      via: "AgentOS.Manifest.load + field comparison (drift warning)"
      pattern: "Manifest.load"
---

<objective>
Build the daily 07:00 trigger and the hard-wired provisioning seam. The Scheduler is a self-rescheduling GenServer (no Quantum, no DB). The Provisioner supplies the one agent's config from code/config — NOT from an arbitrary manifest (v0 is explicitly hard-wired) — and adds a cheap startup consistency check that warns if the hard-wired config drifts from the hand-kept manifest, making the "human-kept-in-sync" promise observable.

Purpose: Implements REQ-trigger-time (the one daily emergence signal) and REQ-instantiate-from-declaration in its v0 form (hard-wired, with a drift safeguard). The Scheduler's `:fire` hook is the integration point Plan 05 wires the run pipeline into.

Output: A `Scheduler` GenServer with testable next-07:00 arithmetic, a `Provisioner` exposing the hard-wired agent config + a manifest-drift check, and hard-wired config in `config/config.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-walking-skeleton-v0/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-v0/01-VALIDATION.md
@.planning/phases/01-walking-skeleton-v0/01-01-PLAN.md

<interfaces>
<!-- From Plan 01 (already built): -->
AgentOS.Manifest.load(path)  # => {:ok, map} | {:error, reason}
# map["spend"]["cap"] (int), map["connectors"]/map["mounts"]/map["outputs"] (lists), map["purpose"] (string)

<!-- Contracts THIS plan establishes for Plan 05: -->
AgentOS.Scheduler.start_link(opts)   # opts: :tz (default config), :run_fn (default &Provisioner.fire_run/0, overridable for tests)
AgentOS.Scheduler.ms_until_next(now_dt, hour)  # PURE function => non-negative integer ms (test seam)
AgentOS.Provisioner.agent_config()   # => %{manifest_path:, agent_cmd:, agent_args:, tz:, run_hour:}
AgentOS.Provisioner.check_drift()     # => :ok | {:drift, [field]}  (logs a warning on drift)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hard-wired Provisioner + manifest-drift consistency check</name>
  <files>config/config.exs, lib/agent_os/provisioner.ex, test/agent_os/provisioner_test.exs</files>
  <read_first>
    - config/config.exs (generated in Plan 01 Task 1 — may be near-empty)
    - lib/agent_os/manifest.ex (Plan 01 — the load/1 contract)
    - manifests/discovery.md (Plan 01 — the 7 fields the drift check compares against)
    - .planning/phases/01-walking-skeleton-v0/01-RESEARCH.md (§ "Pitfall 4: Manifest drift")
  </read_first>
  <behavior>
    - Test: `Provisioner.agent_config/0` returns a map with keys `:manifest_path`, `:agent_cmd`, `:agent_args`, `:tz`, `:run_hour` sourced from `config/config.exs`.
    - Test: `Provisioner.check_drift/0` returns `:ok` when the hard-wired config's declared connectors/outputs/spend-cap match `manifests/discovery.md`.
    - Test: when a config field is deliberately overridden to disagree with the manifest (inject via Application.put_env in the test), `check_drift/0` returns `{:drift, fields}` listing the mismatched field(s).
  </behavior>
  <action>
    Add to `config/config.exs` a `config :agent_os, :agent,` keyword block with: `manifest_path: "manifests/discovery.md"`, `agent_cmd: "uv"`, `agent_args: ["run", "python", "agents/discovery/main.py"]`, `tz: "Etc/UTC"`, `run_hour: 7`, and a declared-grants mirror `connectors: ["record_signal"]`, `outputs: ["append_digest"]`, `spend_cap: 5`. (Per RESEARCH A2, pin the trigger to a single configured tz — UTC — for v0; document that local-wall-clock 07:00 across DST would need the `tz` hex lib, deferred.)

    Create `lib/agent_os/provisioner.ex`:
    - `agent_config/0`: read the `:agent_os, :agent` config into a map with the keys above. This is the HARD-WIRED provisioning (REQ-instantiate-from-declaration R1) — the agent is wired in config, NOT instantiated from an arbitrary manifest.
    - `check_drift/0`: `Manifest.load(agent_config().manifest_path)`; compare manifest `"connectors"`, `"outputs"`, and `"spend"=>"cap"` against the config's `connectors`, `outputs`, `spend_cap`. Return `:ok` if all match; else `Logger.warning("manifest drift: ...")` and return `{:drift, mismatched_fields}`. This makes the human-kept-in-sync promise observable (RESEARCH Pitfall 4).
    - `fire_run/0`: a stub for now that just `Logger.info("run fired")` and returns `:ok` — Plan 05 replaces the body with the real run pipeline (the Scheduler calls this; keep it as the seam).

    Write `test/agent_os/provisioner_test.exs` covering the three behaviors. For the drift test, use `Application.put_env(:agent_os, :agent, ...)` in a setup with an `on_exit` that restores it.
  </action>
  <verify>
    <automated>mix test test/agent_os/provisioner_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/agent_os/provisioner_test.exs` exits 0.
    - `grep -q 'def agent_config' lib/agent_os/provisioner.ex` succeeds.
    - `grep -q 'def check_drift' lib/agent_os/provisioner.ex` succeeds.
    - `grep -q 'Manifest.load' lib/agent_os/provisioner.ex` succeeds.
    - `grep -q 'config :agent_os' config/config.exs` succeeds.
    - `grep -q 'run_hour: 7' config/config.exs` succeeds.
  </acceptance_criteria>
  <done>Hard-wired config drives provisioning; `check_drift/0` detects and warns on config-vs-manifest divergence; 3 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Daily 07:00 self-rescheduling Scheduler GenServer</name>
  <files>lib/agent_os/scheduler.ex, lib/agent_os/application.ex, test/agent_os/scheduler_test.exs</files>
  <read_first>
    - lib/agent_os/application.ex (you add Scheduler to the children)
    - lib/agent_os/provisioner.ex (Task 1 — Scheduler's :fire calls Provisioner.fire_run/0)
    - .planning/phases/01-walking-skeleton-v0/01-RESEARCH.md (§ "Pattern 5: Daily 07:00 self-rescheduling timer", "Pitfall 5: Timezone ambiguity")
  </read_first>
  <behavior>
    - Test (PURE arithmetic): `Scheduler.ms_until_next(~U[2026-06-27 06:00:00Z], 7)` returns exactly 3_600_000 (1h).
    - Test: `Scheduler.ms_until_next(~U[2026-06-27 07:00:00Z], 7)` returns the full-day ms (86_400_000) — at-or-past the hour rolls to tomorrow, never returns 0 or negative.
    - Test: `Scheduler.ms_until_next(~U[2026-06-27 08:00:00Z], 7)` returns 23h in ms (82_800_000).
    - Test: starting the Scheduler with an injected `run_fn` that sends a message to the test pid, and sending it `:fire`, causes `run_fn` to be invoked AND a new timer to be armed (state shows a scheduled ref / next-ms).
  </behavior>
  <action>
    Create `lib/agent_os/scheduler.ex` as a GenServer (DEC-substrate-owns-all-state: scheduling is the substrate's job; triggers are data).
    - `start_link/1` accepts opts: `:run_fn` (default `&AgentOS.Provisioner.fire_run/0`), `:run_hour` (default from `Provisioner.agent_config().run_hour`), `:tz` (default config tz, "Etc/UTC" at v0). Name `__MODULE__`.
    - `ms_until_next/2` — a PURE public function `ms_until_next(now_dt, hour)` (test seam, no process): compute the next DateTime at `hour:00:00` in UTC strictly AFTER `now_dt`; if today's `hour:00` is at-or-before `now_dt`, use tomorrow's. Return `DateTime.diff(target, now_dt, :millisecond)`. MUST be > 0 always (guard against the Pitfall-6/timezone off-by edge: at-or-past rolls forward a day).
    - `init/1`: arm the first timer via `schedule_next/1` (computes ms with `ms_until_next(DateTime.utc_now(), run_hour)` and `Process.send_after(self(), :fire, ms)`), store the timer ref + next-ms + run_fn in state.
    - `handle_info(:fire, state)`: invoke `state.run_fn.()`, then re-arm via `schedule_next/1` and `{:noreply, new_state}` (self-reschedules the next day — REQ-trigger-time).
    - Add `{AgentOS.Scheduler, []}` to the children in `lib/agent_os/application.ex`.

    Write `test/agent_os/scheduler_test.exs` covering the four behaviors. For the `:fire` test, inject `run_fn: fn -> send(test_pid, :fired) end` and `assert_receive :fired`.
  </action>
  <verify>
    <automated>mix test test/agent_os/scheduler_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/agent_os/scheduler_test.exs` exits 0.
    - `grep -q 'Process.send_after' lib/agent_os/scheduler.ex` succeeds.
    - `grep -q 'def ms_until_next' lib/agent_os/scheduler.ex` succeeds.
    - `grep -q 'AgentOS.Scheduler' lib/agent_os/application.ex` succeeds.
    - `grep -v '^#' lib/agent_os/scheduler.ex | grep -q 'handle_info(:fire'` succeeds.
  </acceptance_criteria>
  <done>Scheduler computes next-07:00 ms purely (3 arithmetic cases verified), fires `run_fn` and self-reschedules on `:fire`; mounted in the supervision tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/manifest → runtime | Hard-wired config is the v0 source of provisioning truth; the manifest is human-kept and only compared (not enforced) at v0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-06 | Tampering / Repudiation | manifest-vs-config drift | mitigate | `check_drift/0` deterministically compares hard-wired grants against the hand-kept manifest and logs a loud warning on divergence, making "human-kept-in-sync" observable (defeats silent manifest drift — RESEARCH Pitfall 4). Not enforcement (that is Phase 3), but it is legible. |
| T-1-07 | Denial of Service | Scheduler timer arithmetic | mitigate | `ms_until_next/2` is guarded to always return a strictly-positive interval (at-or-past the hour rolls to tomorrow), preventing a 0/negative `send_after` busy-fire loop. Verified by the at-07:00 boundary test. |
| T-1-08 | Information Disclosure | agent command in config | accept | The agent command/args are non-secret config (no credential at v0; the credential proxy is Phase 3). No secrets pass via argv (RESEARCH Security V12). Accepted at v0. |
</threat_model>

<verification>
- `mix test test/agent_os/scheduler_test.exs` and `mix test test/agent_os/provisioner_test.exs` green.
- Scheduler + Provisioner mounted; `ms_until_next/2` is pure and always positive.
- `check_drift/0` logs and reports config↔manifest divergence.
</verification>

<success_criteria>
- Daily 07:00 self-rescheduling timer with pure, testable next-occurrence arithmetic (REQ-trigger-time).
- Hard-wired provisioning from config (NOT arbitrary-manifest) with an observable drift safeguard (REQ-instantiate-from-declaration R1).
- Scheduler `:fire` hook ready for Plan 05 to wire the real run pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/01-walking-skeleton-v0/01-02-SUMMARY.md`
</output>
